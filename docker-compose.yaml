services:
  api:
    container_name: hearlers-nest-api-dev
    restart: always
    image: node:20.11.0
    working_dir: /usr/src/app
    command: bash -c "yarn install && yarn start:dev"
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
      - /usr/src/app/src/proto
    ports:
      - "50051:50051"
